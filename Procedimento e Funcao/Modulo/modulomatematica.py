def soma (x,y):
    #DOCUMENTAÇÃO
    """
    Função soma que recebe dois valores  x e y como parametros e retorna a soma deles.
    
    Parâmetros:
        - x: Recebe o primeiro valor do operador da adição.
        - y: Recebe o segundo valor do operador da adição.
        
    Retorno:
        A função retorna a soma de x e y.
    
    """
    soma = x + y
    print("O valor da soma pela função do lado de dentro  é: "+ str(soma))
    return soma #POSSUI RETORNO

def subtracao (x,y):
    
    #DOCUMENTAÇÃO
    """
    Função subtracao que recebe dois valores  x e y como parametros e retorna a subtração deles.
    
    Parâmetros:
        - x: Recebe o primeiro valor do operador da subtração.
        - y: Recebe o segundo valor do operador da subtração.
        
    Retorno:
        A função retorna a subtracao de x e y.
    
    """
    subtracao = x - y
    print("O valor da subtração pela função do lado de dentro  é: "+ str(subtracao))
    return subtracao #POSSUI RETORNO

def divisao (x,y):
    
    #DOCUMENTAÇÃO
    """
    Função divisao que recebe dois valores  x e y como parametros e retorna a divisão deles.
    
    Parâmetros:
        - x: Recebe o primeiro valor do operador da divisão.
        - y: Recebe o segundo valor do operador da divisão.
        
    Retorno:
        A função retorna a divisão de x e y.
    
    """
    divisao = x / y
    print("O valor da divisão pela função do lado de dentro  é: "+ str(divisao))
    return divisao #POSSUI RETORNO

def multiplicacao (x,y):
    
    #DOCUMENTAÇÃO
    """
    Função multiplicacao que recebe dois valores  x e y como parametros e retorna a multiplicação deles.
    
    Parâmetros:
        - x: Recebe o primeiro valor do operador da multiplicação.
        - y: Recebe o segundo valor do operador da multiplicação.
        
    Retorno:
        A função retorna a multiplicação de x e y.
    
    """
    multiplicacao = x * y
    print("O valor da multiplicação pela função do lado de dentro  é: "+ str(multiplicacao))
    return multiplicacao #POSSUI RETORNO